

<h1>All posts</h1>

<br>
<div class = "container">
  <% @user = current_user%>
  <%= render "users/menu" %>
  
  <div class="col-lg-10">
	<ul>
	  <% @posts.each do |post| %>
	    <li class="posts">
		   <%= link_to post.title, post,  class:"posts" %>
		   <% if @user.admin?%>
       		 | <%= link_to "delete", post, method: :delete,
                                  data: { confirm: "You sure about dat?" }, class:"posts" %>
      		<% end %>
	    </li>
	  <% end %>
	</ul>
  </div>
</div>